[pytest]
# 只收集 tests/ 目录，避免误收根目录的手动脚本 test_analyzer.py
testpaths = tests
# 把项目根加入 sys.path，使测试能 import src / mcp_server
pythonpath = .
# librosa / numba 等会产生大量无关警告，过滤掉保持输出干净
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
